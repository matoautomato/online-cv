{
  "name": "online-cv",
  "version": "1.0.0",
  "description": "Static CV generator — YAML data + Handlebars template → self-contained HTML",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "test": "node --test 'test/**/*.test.js'"
  },
  "dependencies": {
    "handlebars": "^4.7.8",
    "js-yaml": "^4.1.0"
  }
}
